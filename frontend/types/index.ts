export type Animal = {
    id: number
    name: string
    dateOfBirth: string
    species: string
    breed: string
    color: string
    weight: number
    personId: number
  }

export type Person = {
    id: number
    lastName: string
    firstName: string
    email: string
    phoneNumber: string
}

